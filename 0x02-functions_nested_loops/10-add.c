#include "main.h"

/**
 * add - adds two integers and returns the result
 * @c: the int first parameter
 * @n: the second parameter
 * Return: return the result
 */
int add(int n, int c)
{
return (n + c);
}
