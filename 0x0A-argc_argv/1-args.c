#include <stdio.h>

/**
 * main - prints the number of arguments passed into it
 * @argc: the int bumber of main arguments
 * @argv: the char table of pointer to the string
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc);
return (0);
}
